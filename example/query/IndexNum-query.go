
// Code generated by genmaps.go; DO NOT EDIT.
// template file is https://github.com/kazu/fbshelper/blob/master/template/query.go.tmpl github.com/kazu/fbshelper/template/query.go.tmpl 
//   https://github.com/kazu/fbshelper/blob/master/template/union.query.go.tmpl


package vfs_schema

import (
    flatbuffers "github.com/google/flatbuffers/go"
    base "github.com/kazu/fbshelper/query/base"
    "reflect"
)

const (
    DUMMY_IndexNum = flatbuffers.VtableMetadataFields
)

const (
        IndexNum_Size =     0
        IndexNum_Maps =     1
)

var IndexNum_FieldEnum = map[string]int{
        "Size": IndexNum_Size,
        "Maps": IndexNum_Maps,
}



type FbsIndexNum struct {
	*base.Node
}


type FbsIndexNumMaps struct {
    *base.NodeList
}

func (node FbsIndexNum) SearchInfo(pos int, fn RecFn, condFn CondFn) {

	info := node.Info()

	if condFn(pos, info) {
		fn(base.NodePath{Name: "IndexNum", Idx: -1}, info)
	}else{
        return
    }

	for i := 0; i < node.CountOfField(); i++ {
		if node.IsLeafAt(i) {
			fInfo := base.Info(node.ValueInfo(i))
			if condFn(pos, fInfo) {
				fn(base.NodePath{Name: "IndexNum", Idx: i}, fInfo)
			}
			continue
		}
        switch i {
        case 0:
        case 1:
                node.Maps().SearchInfo(pos, fn, condFn)    
        default:
			base.Log(base.LOG_ERROR, func() base.LogArgs {
				return F("node must be Noder")
			})
        }

	}

}
func (node FbsIndexNum) Info() base.Info {

    info := base.Info{Pos: node.Pos, Size: -1}
    for i := 0; i < len(node.VTable); i++ {
        vInfo := node.ValueInfo(i)
        if info.Pos + info.Size < vInfo.Pos + vInfo.Size {
            info.Size = (vInfo.Pos + vInfo.Size) - info.Pos
        }
    }
    return info    
}


func (node FbsIndexNum) IsLeafAt(i int) bool {
    switch i {
    case 0:
        return true
    case 1:
        return false
    }
    return false
}
func (node FbsIndexNum) ValueInfo(i int) base.ValueInfo {

    switch i {
    case 0:
        if node.ValueInfos[i].IsNotReady() {
            node.ValueInfoPos(i)
        }
        node.ValueInfos[i].Size = base.SizeOfint32
    case 1:
         if node.ValueInfos[i].IsNotReady() {
            node.ValueInfoPosList(i)
        }
        node.ValueInfos[i].Size = node.Maps().Info().Size
     }
     return node.ValueInfos[i]
}


func (node FbsIndexNum) FieldAt(i int) interface{} {

    switch i {
    case 0:
        return node.Size()
    case 1:
        return node.Maps()
     }
     return nil
}


// Unmarsla parse flatbuffers data and store the result
// in the value point to by v, if v is ni or not pointer,
// Unmarshal returns an ERR_MUST_POINTER, ERR_INVALID_TYPE
func (node FbsIndexNum) Unmarshal(v interface{}) error {

    return node.Node.Unmarshal(v, func(s string, rv reflect.Value) error {
        
        switch IndexNum_FieldEnum[s] {
        case IndexNum_Size:
            //return node.Size()
            rv.Set(reflect.ValueOf(  node.Size() ))
        }
        return nil
    })

}




func (node FbsIndexNum) Size() int32 {
    if node.VTable[0] == 0 {
        return int32(0)
    }
    return int32(flatbuffers.GetInt32(node.ValueNormal(0)))
}


func (node FbsIndexNum) Maps() FbsIndexNumMaps {
    if node.VTable[1] == 0 {
        return FbsIndexNumMaps{}
    }
    nodelist :=  node.ValueList(1)
    return FbsIndexNumMaps{
                NodeList: &nodelist,
    }
}




func (node FbsIndexNum) CountOfField() int {
    return 2
}




func (node FbsIndexNumMaps) At(i int) FbsInvertedMapNum {
    if i >= int(node.ValueInfo.VLen) || i < 0 {
		return FbsInvertedMapNum{}
	}

    ptr := int(node.ValueInfo.Pos) + i*4
	return FbsInvertedMapNum{Node: base.NewNode(node.Base, ptr + int(flatbuffers.GetUint32( node.R(ptr) )))}
}


func (node FbsIndexNumMaps) First() FbsInvertedMapNum {
	return node.At(0)
}


func (node FbsIndexNumMaps) Last() FbsInvertedMapNum {
	return node.At(int(node.ValueInfo.VLen)-1)
}

func (node FbsIndexNumMaps) Select(fn func(m FbsInvertedMapNum) bool) []FbsInvertedMapNum {

	result := make([]FbsInvertedMapNum, 0, int(node.ValueInfo.VLen))
	for i := 0; i < int(node.ValueInfo.VLen); i++ {
		if m := node.At(i); fn(m) {
			result = append(result, m)
		}
	}
	return result
}

func (node FbsIndexNumMaps) Find(fn func(m FbsInvertedMapNum) bool) FbsInvertedMapNum{

	for i := 0; i < int(node.ValueInfo.VLen); i++ {
		if m := node.At(i); fn(m) {
			return m
		}
	}
	return FbsInvertedMapNum{}
}

func (node FbsIndexNumMaps) All() []FbsInvertedMapNum {
	return node.Select(func(m FbsInvertedMapNum) bool { return true })
}

func (node FbsIndexNumMaps) Count() int {
	return int(node.ValueInfo.VLen)
}

func (node FbsIndexNumMaps) Info() base.Info {

    info := base.Info{Pos: node.ValueInfo.Pos, Size: -1}
    vInfo := node.Last().Info()



    if info.Pos + info.Size < vInfo.Pos + vInfo.Size {
        info.Size = (vInfo.Pos + vInfo.Size) - info.Pos
    }
    return info
}

func (node FbsIndexNumMaps) SearchInfo(pos int, fn RecFn, condFn CondFn) {

    info := node.Info()

    if condFn(pos, info) {
        fn(base.NodePath{Name: "IndexNum.Maps", Idx: -1}, info)
	}else{
        return
    }

    var v interface{}
    for _, cNode := range node.All() {
        v = cNode
        if vv, ok := v.(Searcher); ok {    
                vv.SearchInfo(pos, fn, condFn)
        }else{
            goto NO_NODE
        }
    }
    return
    

NO_NODE:     
    for i := 0 ; i < int(node.ValueInfo.VLen) ; i++ {
        ptr := int(node.ValueInfo.Pos) + i*4
        start := ptr + int(flatbuffers.GetUint32( node.R(ptr) ))
        size := info.Size
        if i + 1 < int(node.ValueInfo.Pos) {
            size = ptr+4 + int(flatbuffers.GetUint32( node.R(ptr+4) )) - start
        }
        cInfo := base.Info{Pos: start, Size: size}
        if condFn(pos, info) {
            fn(base.NodePath{Name: "IndexNum.Maps", Idx: i}, cInfo)
        }
    }
}
